class Solution {
public:
    int findLUSlength(string a, string b) {
        if(a==b)
        return -1;
        if(a.length()==b.length()&&a!=b)
        return a.length();
        if(a.length()>b.length()&&a!=b)
        return a.length();
        if(b.length()>a.length()&&a!=b)
        return b.length();
        return -1;
    }
};
