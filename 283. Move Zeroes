class Solution {
public:
    void moveZeroes(vector<int>& nums) {
        vector<int> v(nums.size(),0);
        int a=0;
        for(int i=0;i<nums.size();i++){
            if(nums[i]!=0){
                v[a]=nums[i];
                a++;
            }
        }
        nums=v;
    }
};
