class Solution {
public:
    bool isPerfectSquare(int num) {
        bool ans;
        long i;
        for(i=1;i*i<=num;i++){
            if(num%(i*i)==0&&num/(i)==i){
                ans=true;
            break;}
            else
                ans=false;
        }
        return ans;
    }
};
